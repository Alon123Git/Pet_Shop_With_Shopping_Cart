@using Models;
@model Models.CoverType
@{
    ViewData["Title"] = "Details";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
<link rel="stylesheet" href="~/css/Details.css" />
<link rel="stylesheet" href="~/css/SiteLayout.css" />

<body>
    <div class="heroTitle">
        <h2 class="title-text">Details</h2>
    </div>
    <div>
        <h4>Animal</h4>
        <hr />

        @*Display the details about one animal: *@
        <dl id="bigDl" class="dl-horizontal">
            <div class="callDiv" id="leftDiv">

                <dt>
                    @Html.DisplayNameFor(model => model.Name)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Name)
                </dd>
            </div>

            @*Add an comment: *@
            @*@<div class="callDiv" id="rightDiv">
            @using (Html.BeginForm())
            {
            @<dt>
            @Html.LabelFor(model => model.comm)
            <input class="form-control" placeholder="Add comment.." type="text" name="comment" />
            <br />
            <input type="submit" class="btn btn-primary" value="Submit comment" />
            </dt>
            }

            <dt>
            <h4>Comments:</h4>
            <ol id="commants">
            @foreach (var item in Model.comm)
            {
            <li>@item.Comment1</li>

            }
            </ol>
            </dt>
            </div>*@
        </dl>
    </div>
</body>